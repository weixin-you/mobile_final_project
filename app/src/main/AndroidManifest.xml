<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cst2335.mobilefinalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileFinalProject"
        tools:targetApi="31">
        <activity
            android:name=".DisplayData"
            android:exported="true"
            android:label="@string/displaydata" />
        <activity
            android:name=".FoodNutritionDatabase"
            android:exported="true"
            android:label="@string/food" />
        <activity
            android:name=".CocktailDatabase"
            android:exported="true"
            android:label="@string/cocktail" />
        <activity
            android:name=".OwlbotDictionary"
            android:exported="true"
            android:label="@string/owlbot" />
        <activity
            android:name=".Pexels"
            android:exported="true"
            android:label="@string/Pexels" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>